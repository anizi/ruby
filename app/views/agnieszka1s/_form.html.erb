<%= form_for(@agnieszka1) do |f| %>
  <% if @agnieszka1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agnieszka1.errors.count, "error") %> prohibited this agnieszka1 from being saved:</h2>

      <ul>
      <% @agnieszka1.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :imie %><br />
    <%= f.text_field :imie %>
  </div>
  <div class="field">
    <%= f.label :nazwisko %><br />
    <%= f.text_field :nazwisko %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
